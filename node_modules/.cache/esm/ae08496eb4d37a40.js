_b4d‍.x([["default",()=>_b4d‍.o]]);class Library {
  constructor(books = []) {
    this.books = books;
  }

  bookCount() {
    return this.books.length;
  }

  addBook(newBook) {
    this.books.push(newBook);
  }

  addBooks(newBooks) {
    newBooks.forEach((book) => this.books.push(book));
  }

  printInventory() {
    this.books.forEach((book) => {
      const { title, author } = book;
      console.log(`${title} by ${author}`);
    });
  }

}

_b4d‍.d(Library);
